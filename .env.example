# Copy this file to .env and adjust if ports conflict
# docker compose reads .env automatically

# Unique project name (prevents container name clashes when running multiple stacks)
COMPOSE_PROJECT_NAME=tourplanner-angular

# Host ports (change if already in use)
POSTGRES_PORT=5433
PGADMIN_PORT=5051
API_PORT=7103
UI_PORT=7227
